  GNU nano 6.2                                         docker-compose.yml                                                   
version: "3.7"

services:
  kasm:
    image: lscr.io/linuxserver/kasm:latest
    user: 1000:1000
    restart: unless-stopped
    environment:
      DOCKER_HUB_PASSWORD: PASS
      DOCKER_HUB_USERNAME: USER
      DOCKER_MTU: 1500
      KASM_PORT: 443
      APP_HOST: cloudrack-kasm_kasm_1
      APP_PORT: 443
      PROXY_AUTH_ADD: "false"
    volumes:
      - cloudrack-kasm-data:/opt
      - cloudrack-kasm-profiles:/profiles
    restart: unless-stopped
    networks:
      - kasm
    ports:
      - target: 3000
        published: "3022"
        protocol: tcp
      - target: 443
        published: "443"
        protocol: tcp
networks:
  kasm:
    name: kasm
volumes:
  cloudrack-kasm-data:
  cloudrack-kasm-profiles:
